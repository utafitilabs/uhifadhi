{# STATIONS & WHO IS ON — the slot the roster's card will fill.

   Design: areas/<area>/overview.widgets.js (RO·C1, the roster's own list).

   THE SLOT IS HELD OPEN ON PURPOSE. The design's composition has a roster
   card here; this installation's roster does not publish one yet. Dropping
   the cell would leave the row half empty and say nothing about why — and
   the moment roster contributes its own widget, that cell takes this one's
   place and this one goes.

   AN ABSENCE IS ONE LINE. It was three sentences and a nested box, which is
   an essay about a card that has nothing in it. #}
<div class="c">
    <span class="tab">Stations &amp; who is on<span class="src">&middot; awaiting the roster</span></span>
    <span class="ao-by next"><i></i>the roster module</span>
    <div class="use" style="margin:0 2px">Nothing to show until the roster module is installed.</div>
    {% if door('modules.read', area) %}
        <a class="more" href="{{ path('area_modules', {uuid: area.uuidString}) }}">The Modules tab &rarr;</a>
    {% endif %}
</div>
