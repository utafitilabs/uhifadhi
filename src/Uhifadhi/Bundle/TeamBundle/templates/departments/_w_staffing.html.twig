{# DP·C1 — ranked bars, longest first, scaled to the largest
   department so the comparison the card exists to make is the one
   a reader gets. #}
<div class="c">
    <span class="tab">Positions filled by department<span class="src">&middot; longest first</span></span>
    {{ atlas_bars(staffing) }}
    <div class="sxmore"><span>{{ staffing.bars|length }} of {{ staffing.bars|length }} departments &middot; nothing hidden</span><span class="sp"></span><a href="{{ path('team_departments') }}">The register &rarr;</a></div>
</div>
