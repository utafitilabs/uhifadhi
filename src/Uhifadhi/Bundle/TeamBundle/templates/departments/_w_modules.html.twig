{# DP·C3 — how many of the installed modules each department reads. #}
<div class="c">
    <span class="tab">Modules per department<span class="src">&middot; of the installed catalogue</span></span>
    {{ atlas_bars(modules) }}
    <div class="sxmore"><span>{{ modules.bars|length }} of {{ modules.bars|length }} departments</span><span class="sp"></span><a href="{{ path('team_departments_modules') }}">The matrix &rarr;</a></div>
</div>
