{# THE ROSTER'S SHARED VOCABULARY — the marks every direction draws a person with.

   THE DESIGN UNROLLS THESE AND THIS DOES NOT, and that is the one deliberate
   difference between the two. In the workspace each direction is a standalone
   HTML file with every row written out, because a static page has no other way
   to be looked at; here six directions render the same twelve people, and six
   copies of "how a person is drawn" is six places for the tier pill to drift.

   The marks themselves are unchanged: same classes, same order, same words. A
   direction that wants a cell the others do not — b's band rows, f's department
   column — writes that itself and takes the rest from here.

   ICONS ARE INLINE LUCIDE SVGs, never emoji, and they live in macros for the
   same reason the cells do. #}

{% macro icon_check() %}<svg width="17" height="17" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6 9 17l-5-5"/></svg>{% endmacro %}
{% macro icon_clock() %}<svg width="17" height="17" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="9"/><path d="M12 7v5l3 2"/></svg>{% endmacro %}
{% macro icon_off() %}<svg width="17" height="17" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><path d="m4.9 4.9 14.2 14.2"/></svg>{% endmacro %}
{% macro icon_info() %}<svg width="17" height="17" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><path d="M12 8v4"/><path d="M12 16h.01"/></svg>{% endmacro %}
{% macro icon_warn() %}<svg width="17" height="17" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round"><path d="m21.73 18-8-14a2 2 0 0 0-3.48 0l-8 14A2 2 0 0 0 4 21h16a2 2 0 0 0 1.73-3"/><path d="M12 9v4"/><path d="M12 17h.01"/></svg>{% endmacro %}
{% macro icon_mail() %}<svg width="17" height="17" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round"><rect width="20" height="16" x="2" y="4" rx="2"/><path d="m22 7-8.97 5.7a1.94 1.94 0 0 1-2.06 0L2 7"/></svg>{% endmacro %}
{% macro icon_shield() %}<svg width="17" height="17" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M20 13c0 5-3.5 7.5-7.66 8.95a1 1 0 0 1-.67-.01C7.5 20.5 4 18 4 13V6a1 1 0 0 1 1-1c2 0 4.5-1.2 6.24-2.72a1.17 1.17 0 0 1 1.52 0C14.51 3.81 17 5 19 5a1 1 0 0 1 1 1z"/></svg>{% endmacro %}
{% macro icon_people(stroke) %}<svg width="17" height="17" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="{{ stroke|default(2) }}" stroke-linecap="round" stroke-linejoin="round"><path d="M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M22 21v-2a4 4 0 0 0-3-3.87"/><path d="M16 3.13a4 4 0 0 1 0 7.75"/></svg>{% endmacro %}
{% macro icon_plus() %}<svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14"/><path d="M12 5v14"/></svg>{% endmacro %}
{% macro icon_tag() %}<svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="m15.5 7.5 2.3 2.3a1 1 0 0 0 1.4 0l2.1-2.1a1 1 0 0 0 0-1.4L19 4"/><path d="m21 2-9.6 9.6"/><circle cx="7.5" cy="15.5" r="5.5"/></svg>{% endmacro %}
{% macro icon_grid() %}<svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect width="7" height="7" x="3" y="3" rx="1"/><rect width="7" height="7" x="14" y="3" rx="1"/><rect width="7" height="7" x="14" y="14" rx="1"/><rect width="7" height="7" x="3" y="14" rx="1"/></svg>{% endmacro %}
{% macro icon_back() %}<svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="m15 18-6-6 6-6"/></svg>{% endmacro %}
{% macro icon_eye() %}<svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M2.062 12.348a1 1 0 0 1 0-.696 10.75 10.75 0 0 1 19.876 0 1 1 0 0 1 0 .696 10.75 10.75 0 0 1-19.876 0"/><circle cx="12" cy="12" r="3"/></svg>{% endmacro %}
{% macro icon_undo() %}<svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M3 7v6h6"/><path d="M21 17a9 9 0 0 0-9-9 9 9 0 0 0-6 2.3L3 13"/></svg>{% endmacro %}
{% macro icon_save() %}<svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M15.2 3a2 2 0 0 1 1.4.6l3.8 3.8a2 2 0 0 1 .6 1.4V19a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2z"/><path d="M17 21v-7a1 1 0 0 0-1-1H8a1 1 0 0 0-1 1v7"/><path d="M7 3v4a1 1 0 0 0 1 1h7"/></svg>{% endmacro %}
{% macro icon_search() %}<svg width="17" height="17" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="11" cy="11" r="7"/><path d="m21 21-4.35-4.35"/></svg>{% endmacro %}

{# Two letters, because a roster of twelve faces nobody has uploaded is a roster
   of twelve identical grey circles. #}
{% macro initials(user) %}{{ (user.firstName|default('?')|slice(0, 1) ~ user.lastName|default('?')|slice(0, 1))|upper }}{% endmacro %}

{% macro tier_pill(tier) %}
    {%- set klass = {'super_admin': 't-super', 'admin': 't-admin', 'staff': 't-staff'} -%}
    <span class="tier {{ klass[tier.value] }}">{{ tier.label }}</span>
{% endmacro %}

{# THE PERSON CELL, and it does not wrap. A name broken across two lines in a
   dense table is the one thing that makes the table stop reading as rows. #}
{% macro person(user, isYou) %}
    {% import _self as bits %}
    <div class="who">
        <span class="avatar sm">{{ bits.initials(user) }}</span>
        <b>{{ user.fullName }}</b>
        {%- if isYou %}<span class="tm-you">you</span>{% endif -%}
    </div>
    <a class="tm-mail" href="mailto:{{ user.email }}">{{ user.email }}</a>
{% endmacro %}

{# THE THREE-LINE POSITION CELL: the name, then what it grants, then the
   administrator mark if the position carries team.manage.

   THE NAME IS THE WHOLE OF IT. It used to be written department-first,
   because two departments could each own the word "Analyst" and a bare name
   was ambiguous by construction. A position belongs to no department now and
   its name is unique across the organization, so there is one Analyst and the
   word disambiguates itself.

   AND IT IS NOT A COUNT FOR THE TWO TOP TIERS. A Super Admin's position may
   hold eight permissions and they decide nothing for that person: the voter
   grants by tier before it ever looks at a position, so a count here would name
   the wrong cause. It reads "everything, by tier" instead. #}
{% macro position_cell(user, positionsPath) %}
    {% import _self as bits %}
    {%- set position = user.position -%}
    {%- if position is null -%}
        <span class="tm-nopos">{{ bits.icon_info() }}no position</span>
        {%- if user.teamRole.canManageContent -%}
            <span class="tm-grants">everything, by tier</span>
        {%- else -%}
            <span class="tm-grants none">holds nothing</span>
        {%- endif -%}
    {%- else -%}
        <a class="tm-pos qual" href="{{ positionsPath }}">
            <span class="n">{{ position.name }}</span>
        </a>
        {%- if user.teamRole.canManageContent -%}
            <span class="tm-grants">everything, by tier</span>
        {%- else -%}
            {%- set granted = position.grantValues|length -%}
            <span class="tm-grants{{ granted ? '' : ' none' }}">{{ granted ? granted ~ ' granted' : 'holds nothing' }}</span>
        {%- endif -%}
        {%- if 'directory.manage' in position.grantValues -%}
            <span class="tm-mgr">{{ bits.icon_shield() }}directory.manage</span>
        {%- endif -%}
    {%- endif -%}
{% endmacro %}

{% macro ranger_code(user) %}
    {%- if user.rangerCode -%}<span class="tm-code">{{ user.rangerCode|upper }}</span>
    {%- else -%}<span class="tm-code none">&mdash;</span>{%- endif -%}
{% endmacro %}

{# THE ACCOUNT CELL — TWO INDEPENDENT AXES, and the pills never merge.
   active/deactivated is one fact and verified/never-signed-in is another; a
   person can be any combination, and the awkward one is both at once.

   THE INVITATION LINE RIDES BESIDE "never signed in" AND NOWHERE ELSE. Once
   somebody has signed in, the invitation has done its job and stops being news.
   Its null is meaningful: an account created with a password was never invited
   by anybody, so it says so by saying "created directly". #}
{% macro account_cell(user) %}
    {% import _self as bits %}
    {%- set deactivated = not user.isActive -%}
    {%- set arrived = user.isVerified -%}
    {%- if deactivated or not arrived -%}<span class="tm-accts">{% endif -%}

    {%- if deactivated -%}
        <span class="tm-acct off">{{ bits.icon_off() }} Deactivated</span>
    {%- endif -%}

    {%- if arrived and not deactivated -%}
        <span class="tm-acct ok">{{ bits.icon_check() }}Verified</span>
    {%- elseif not arrived -%}
        <span class="tm-acct new">{{ bits.icon_clock() }}Never signed in</span>
        {%- if user.wasInvited -%}
            <span class="tm-inv">invited by <b>{{ user.invitedBy ? user.invitedBy.fullName : 'somebody since removed' }}</b> &middot; {{ bits.ago(user.invitedAt) }}</span>
        {%- else -%}
            <span class="tm-inv none">created directly &middot; no invitation</span>
        {%- endif -%}
    {%- elseif deactivated -%}
        <span class="tm-acct ok">{{ bits.icon_check() }}Verified</span>
    {%- endif -%}

    {%- if deactivated or not arrived -%}</span>{% endif -%}
{% endmacro %}

{# WHEN AN INVITATION WENT OUT, coarse on purpose: whether it went out on
   Tuesday afternoon or Tuesday evening is not a fact anybody acts on; whether it
   went out three days ago or three months ago is.

   IT IS NOT A `<time>`, DELIBERATELY. A relative reading is a duration, not a
   wall-clock: "three days ago" is three days ago in every timezone there is, so
   there is nothing here for the frame to put right and an absolute stamp in its
   place would lose the only reading anybody acts on. The exact instant rides in
   the title for whoever wants it. #}
{% macro ago(when) %}
    {%- if when is null -%}&mdash;
    {%- else -%}<span title="{{ when|date('c') }}">
        {%- set days = date().diff(when).days -%}
        {%- if days == 0 -%}today
        {%- elseif days == 1 -%}yesterday
        {%- elseif days < 31 -%}{{ days }} days ago
        {%- elseif days < 365 -%}{{ (days / 30)|round(0, 'floor') }} months ago
        {%- else -%}{{ (days / 365)|round(0, 'floor') }} years ago
        {%- endif -%}
    </span>{%- endif -%}
{% endmacro %}

{# The row's one action, and its WORD changes with the state: a deactivated
   account's most useful next step is the one that undoes it. #}
{% macro open_button(user) %}
    <a class="open-btn" href="{{ path('team_member', {uuid: user.uuidString}) }}">{{ user.isActive ? 'Open' : 'Reactivate' }}</a>
{% endmacro %}
